databases:
  - name: financas-db
    plan: free
    databaseName: financas_db
    user: financas_db

services:
  - type: web
    name: FinancasPessoais
    runtime: python
    plan: free
    buildCommand: bash build.sh
    startCommand: gunicorn SaudeFinanceira.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.4"
      - key: DJANGO_DEBUG
        value: "false"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: financas-db
          property: connectionString
      - key: ALLOWED_HOSTS
        value: financaspessoais-eloo.onrender.com,.onrender.com
      - key: CSRF_TRUSTED_ORIGINS
        value: https://financaspessoais-eloo.onrender.com
